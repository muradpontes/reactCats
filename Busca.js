import React, { useState } from 'react'
import { View, Text, Button } from 'react-native'
export default function Busca (props) {
    return (
        <View>
        <Text>novo componente tipo {props.tipo}</Text>
        </View>
    )

}